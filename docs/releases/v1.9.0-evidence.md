# odoo-pulse 1.9.0 — release verification evidence

This document records the actual values observed while publishing v1.9.0. It
is the durable record referenced by `docs/guides/releasing.md`; every value
below was read from a live channel or a completed CI run, not copied from an
earlier draft.

## Identity

| | |
|---|---|
| Final version / tag | `1.9.0` / `v1.9.0` |
| Final tag object → commit | `76a5817bb847eed61bc98b7343adf8eb0faf024d` → `a32f6d99baab3bc1006742ee7199b75c1f66276b` |
| Parent RC tag / commit | `v1.9.0rc1` (tag object `dcc5e59a5c7e6530f40023d1f77b1023014fdb99`) → `90ff8517757d59ce03d50e39a07cd31a783bf55d` |
| RC → final diff | 4 files / 5 lines, all version-metadata fields; `git diff --exit-code v1.9.0rc1 -- src/ tests/` clean |

## RC soak window

- **Start:** `2026-07-28T09:12:52Z`
- **End:** `2026-07-30T02:32:00Z`
- **Duration:** **41h19m08s**

This is an **owner-approved deviation** from the 48-hour soak target, decided
by the maintainer. The soak was not rounded up to ≥48h and was not extended
retroactively.

**Separately:** a scheduled `playground-smoke` cron run
([run 30517510340](https://github.com/minhhq-a1/odoo-pulse/actions/runs/30517510340),
`smoke (odoo:19)` completed `2026-07-30T05:48:15Z`, `smoke (odoo:18)` completed
`2026-07-30T05:47:24Z`, both against RC commit `90ff8517757d59ce03d50e39a07cd31a783bf55d`,
both green) ran after the soak window above ended. This is **post-soak
verification only** and is not counted as part of, or used to extend, the
41h19m08s soak duration.

No Critical or Important runtime, packaging, schema, or compatibility issue
was found during or after the soak window. No `rcN+1` cycle was required.

## Local gates on the final commit (`a32f6d9`)

- `python3 scripts/release/sync_version.py --check` — pass
- `ruff check .` — clean
- `pytest -q` — **663 passed** (reconciled from the plan's final target of
  643; no test was deleted: +65 vs the 1.8.2 baseline of 598 and +20 vs the
  planned target, from additional release-hardening coverage)
- `python -m build` + `python -m twine check` — both wheel and sdist pass
  (`odoo_pulse-1.9.0-py3-none-any.whl`, `odoo_pulse-1.9.0.tar.gz`)
- Clean-wheel surface probe: default groups **31 tools / 1 resource** (with
  instructions), all groups **88 tools / 1 resource**
- `git diff --check` — whitespace clean

## Remote CI on the final commit (`a32f6d99baab3bc1006742ee7199b75c1f66276b`)

| Check | Run | Result |
|---|---|---|
| Python 3.10–3.13 + MCP floor (`mcp==1.3.0`) | [30524525371](https://github.com/minhhq-a1/odoo-pulse/actions/runs/30524525371) | ✅ all green |
| Odoo 18 & 19 playground | [30524613159](https://github.com/minhhq-a1/odoo-pulse/actions/runs/30524613159) | ✅ headSha verified, both green |
| Docker preflight (`push_image=false`) | [30525302756](https://github.com/minhhq-a1/odoo-pulse/actions/runs/30525302756) | ✅ build-probe success, push job skipped, nothing published |
| Release orchestrator (tag-triggered) | [30527031328](https://github.com/minhhq-a1/odoo-pulse/actions/runs/30527031328) | ✅ validate → build → publish-pypi → docker/build-probe → docker/push_image → release-record, all success |
| MCP Registry publish | [30527584986](https://github.com/minhhq-a1/odoo-pulse/actions/runs/30527584986) | ✅ explicit-ref checkout → stable validation → server validation → live-PyPI check → OIDC login → publish, all success |

## Ecosystem validators (actual final files)

| Validator | Version | Result |
|---|---|---|
| `@anthropic-ai/mcpb validate manifest.json` | 2.1.2 | pass (schema + icon) |
| `claude plugin validate .` | Claude Code 2.1.220 | pass |
| `mcp-publisher validate server.json` | 1.8.0 | pass |

## Artifact hashes

| File | SHA-256 |
|---|---|
| `odoo_pulse-1.9.0-py3-none-any.whl` | `8ebdf97c5cffb619c441c06766cfa4505efc4c7acea45fcd2939cb277d56014c` |
| `odoo_pulse-1.9.0.tar.gz` | `5a135561b2db864c6163365235cc5879170e81213ef8f9ac0bb530b3542323ea` |

Source: GitHub Release `SHA256SUMS.txt`, cross-checked against PyPI's own
`digests.sha256` for both files — identical.

## GHCR

- Digest of `ghcr.io/minhhq-a1/odoo-pulse:1.9.0`: `sha256:98867ad5183b5c168fb29d6890b846a3f8d98a281b189bf2b14f6293d72655df`
- `1.9.0`, `1.9`, `1`, and `latest` all resolve to this same digest
  (verified with `docker buildx imagetools inspect`)
- Container 31/1 probe against the pulled `:1.9.0` image: pass

## First-party channel URLs

| Channel | URL |
|---|---|
| PyPI | https://pypi.org/project/odoo-pulse/1.9.0/ |
| GitHub Release | https://github.com/minhhq-a1/odoo-pulse/releases/tag/v1.9.0 |
| MCP Registry | https://registry.modelcontextprotocol.io/v0/servers?search=odoo-pulse (`io.github.minhhq-a1/odoo-pulse`, version `1.9.0`, `isLatest: true`, `status: active`, published `2026-07-30T08:40:20Z`) |

`gh api repos/minhhq-a1/odoo-pulse/releases/latest --jq .tag_name` → `v1.9.0`.
`gh release view v1.9.0 --json isPrerelease` → `false`.

## External installation verification (outside the repository checkout)

| Channel | Version | Default (31/1) | All groups (88/1) |
|---|---|---|---|
| PyPI (fresh venv, `pip install odoo-pulse==1.9.0`) | `1.9.0` | ✅ (with instructions) | ✅ |
| `uvx`/`uv run --with odoo-pulse==1.9.0` (ephemeral env) | `1.9.0` | ✅ (with instructions) | ✅ |
| GHCR (`docker pull ghcr.io/minhhq-a1/odoo-pulse:1.9.0`) | — | ✅ | not probed (container probe run at default groups only) |

## Issue / blocker disposition

No blocker was opened against the RC. No `rcN+1` cycle occurred — `v1.9.0rc1`
promoted directly to `v1.9.0`. The only recorded deviation from the plan's
default process is the owner-approved 41h19m08s soak window documented above.

## Tag immutability

`v1.9.0rc1` and `v1.9.0` were each created exactly once and have not been
moved, deleted, or recreated. This evidence commit is a documented descendant
of the `v1.9.0` tag commit and does not move the tag.
